- sudo:
    - shell:
        -
            command: python3 package_installer.py
            description: Install base apps
            stdin: true
            stdout: true
            stderr: true

- shell:
    -
        command: python defaults.py
        description: Set system defaults
        stdout: true
        stderr: true
